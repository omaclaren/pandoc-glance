# Goal
Publish v0.3.0 after the owner's explicit approval, verify the registry package, and push its release tag.

# Current State
- Released `pandoc-glance@0.3.0`; npm `latest` is 0.3.0.
- Annotated tag `v0.3.0` pushed to origin at `1baec3777cf22b0dac5ce8aac27bb998108177f6` (matches registry gitHead).
- Published tarball shasum: `9231ceb66e4b57ec243a6cf201da051efabd53a7`, matching the prepublication packed build.
- Prepublication: typecheck, build, 65 tests, production audit (0 vulnerabilities), packed-install browser smoke all passed.
- Postpublication: fresh-cache registry install, CLI version, packaged annotation module/filter, one-shot/watch browser rendering, inline math/link, 9 note highlights, literal code, light/dark blue styling, unchanged headings, narrow wrapping, saves and script-injection/CSP checks all passed.
- npm initially needed fresh web authentication. Owner ran staged `npm login --auth-type=web && npm publish` in a visible Muxy terminal. Registry took a short time to reflect publication; verification then succeeded. Authentication pane closed, smoke browser/server processes closed. Do not read npm credential files.
- Global `pandoc-glance` remains the registry-installed v0.2.1; publishing did not upgrade it.

# Decisions
No further feature work or CI setup planned. See earlier context notes for annotation design and styling. Pi references remain read-only.

# Next Steps
Owner can update the global command with `npm install --global pandoc-glance@0.3.0`, or request that explicitly. Otherwise use the package and respond to actual bugs.

# Continuation Prompt
v0.3.0 is published and verified, tag pushed at 1baec37. Check git status/log before further changes. Global command was deliberately left at v0.2.1 during this release; do not assume it is current.
