# Goal
Ship v0.3.1 as-is after owner approval, without expanding HTML compatibility further.

# Current State
- Published `pandoc-glance@0.3.1`; npm `latest` is 0.3.1.
- Annotated tag `v0.3.1` pushed at `b6156455d6a41763c532568fcb9b64880370fa4f`, matching registry gitHead.
- Published tarball shasum `9821368bd26318fb92b85cc58902051b1c6466f9` matches the prepublication packed build.
- Prepublication checks: typecheck/build, all 76 tests, production audit (0 vulnerabilities), packed-install browser smoke passed.
- Fresh-cache registry installation verified: CLI 0.3.1, required packaged modules/filter, real Brave one-shot/watch rendering, 9 superscripts/2 subscripts, correct vertical alignment, literal code, annotations, live updates, and hostile HTML remaining inert. No source edits or browser errors.
- Owner completed npm publish authorization in a visible Muxy terminal. Temporary terminal and smoke browser/watch server closed.
- Global `pandoc-glance` remains registry-installed v0.3.0; release did not change it.

# Decisions
Bare sup/sub only. No general raw HTML; `<br>` and other HTML compatibility deferred until needed. Pi references remain read-only. Implementation details: `context/context_20260918_224158_superscript-v031.md`.

# Next Steps
Owner may request the global update, or run `npm install --global pandoc-glance@0.3.1`. Otherwise no further development needed unless real use uncovers a bug.

# Continuation Prompt
v0.3.1 is published and verified, tag pushed at b615645. Global command still v0.3.0. Check git status/log before further changes; keep scope narrow.
